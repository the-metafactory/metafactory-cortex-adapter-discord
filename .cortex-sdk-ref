# Pinned cortex ref the surface-sdk .d.ts is synced from (cortex#1950).
# Bump to adopt a new SURFACE_SDK_VERSION; see scripts/sync-surface-sdk.ts.
#
# TEMPORARY — pinned to the feat/1797-extract-discord branch's "invert
# discord adapter" commit (S12 inversion) because that slice widened
# AdapterSystemEventPort with degraded()/untrustedBotDenied() (Discord needs
# both; slack's port only had recovered()/disconnected()) and this bundle's
# src/index.ts/src/plugin.ts need those two methods. Re-pinned 2026-07-13
# from bfb9466c to 1bf2451e after the cortex branch was rebased onto a newer
# main (dfc62dda XDG wave 4) and force-pushed — bfb9466c is no longer
# reachable from any branch and would eventually GC. The orchestrator MUST
# repin this to the post-merge squash SHA on cortex main once cortex#1797
# (S12) lands — same GC-doom avoidance the slack/mattermost bundles' history
# already documents (do not leave a branch SHA pinned after merge; branch
# tips get GC'd).
1bf2451ed26c0afb3209dbe3d257e41ce16215bf
