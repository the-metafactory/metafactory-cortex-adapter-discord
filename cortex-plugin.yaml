# cortex#1797 (S12, ADR-0024) — plugin manifest for the cortex Discord surface
# adapter bundle.
#
# `id` MUST equal the exported AdapterPlugin's `platform` field (the S6
# shadow-fix pin, src/adapters/loader.ts's loadOneBundle stage (f)) — discord's
# platform is "discord", so id is "discord" here too.
kind: adapter
id: discord
entry: ./src/plugin.ts
sdkRange: "^1"
cortex: ">=6.7.0"
